{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                cancelReport = id; 
                displayHelp = id; 
                showReportWindow = id; 
                submitReport = id; 
            }; 
            CLASS = KTAbstractBugReporter; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                oAccessoryView = NSView; 
                oGenericProgressIndicator = NSProgressIndicator; 
                oGenericProgressTextField = NSTextField; 
                oGenericProgressView = NSView; 
                oOpenSavedFeedbackSwitch = NSButton; 
                oReportWindow = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = KSEmailAddressComboBox; LANGUAGE = ObjC; SUPERCLASS = NSComboBox; }, 
        {
            CLASS = KTExceptionReporter; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                oAddressComboBox = KSEmailAddressComboBox; 
                oDontReportButton = NSButton; 
                oIncludeConsoleCheckbox = NSButton; 
                oInformativeTextField = NSTextField; 
                oMessageTextField = NSTextField; 
                oReportButton = NSButton; 
                oReportTextView = NSTextView; 
            }; 
            SUPERCLASS = KTAbstractBugReporter; 
        }, 
        {CLASS = KTFeedbackAttachment; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}